<a class="btn btn-info pull-right" style="margin-left: 20px;" href="<?=
    $this->url('stuki-workspace/master');
?>"><i class="icon-code-fork"></i> Master</a>

<a class="btn btn-info pull-right" href="<?=
    $this->url('stuki-workspace/firehose');
?>"><i class="icon-bullseye"></i> Firehose</a>


<?php
    use Doctrine\ORM\Mapping\ClassMetadataInfo;
?>

<h1>Workspace One To Many Relations</h1>

<table class="table table-striped table-hover table-bordered">
    <thead>
        <tr>
            <th>Revision</th>
            <th>Entity</th>
            <th>Comment</th>
            <th>User</th>
            <th>Approval</th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td>
                <a href="<?=
                    $this->url('stuki-workspace/revision',
                        array(
                            'revisionId' => $this->revisionEntity->getRevision()->getId()
                        )
                    );
                ?>">

                    <?= $this->revisionEntity->getRevision()->getId(); ?>

                    @ <?php echo $this->StukiDateTimeFormatter($revisionEntity->getRevision()->getTimestamp()); ?>
                </a>

            </td>

            <td>
                <?= $this->stukiRevisionEntityLink($revisionEntity); ?>
            </td>

            <td><?= $this->escapeHtml($this->revisionEntity->getRevision()->getComment()); ?></td>

            <td>
                <?= $this->partial(
                    'stuki-workspace/index/partial/user-button',
                    array('user' => $this->revisionEntity->getRevision()->getUser())
                );?>
            </td>
            <td>
                <?= $this->partial(
                    'stuki-workspace/index/partial/revision-approve',
                    array('revision' => $this->revisionEntity->getRevision())
                );
                ?>
            </td>
        </tr>
    </tbody>
</table>

<?php

    $paginator = $this->stukiOneToManyPaginator(
        $this->page,
        $this->revisionEntity,
        $this->joinTable,
        $this->mappedBy
    );
?>

<table class="table table-striped table-hover table-bordered">
    <thead>
        <tr>
            <th colspan="4">
            <?php echo $this->paginationControl($paginator,
                'Sliding',
                'stuki-workspace/pagination/one-to-many', array('route' => 'stuki-workspace/one-to-many'));
            ?>
            </th>
        </tr>
        <tr>
            <th>Revision</th>
            <th>Entity</th>
            <th>Comment</th>
            <th>User</th>
            <th>Approval</th>
        </tr>
    </thead>

    <tfoot>
        <th colspan="4">
            <?php echo $this->paginationControl($paginator,
                'Sliding',
                'stuki-workspace/pagination/one-to-many', array('route' => 'stuki-workspace/one-to-many'));
            ?>
        </th>
    </tfoot>
    <tbody>
    <?php
    foreach ($paginator as $association) {
    ?>
        <tr>
            <td>
                <a href="<?=
                    $this->url('stuki-workspace/revision',
                        array(
                            'revisionId' => $association->getRevision()->getId()
                        )
                    );
                ?>">

                    <?= $association->getRevision()->getId(); ?>

                    @ <?php echo $this->StukiDateTimeFormatter($association->getRevision()->getTimestamp()); ?>
                </a>

            </td>

            <td>
                <?= $this->stukiRevisionEntityLink($association); ?>
            </td>

            <td><?= $this->escapeHtml($association->getRevision()->getComment()); ?></td>

            <td>
                <?= $this->partial(
                    'stuki-workspace/index/partial/user-button',
                    array('user' => $association->getRevision()->getUser())
                );?>
            </td>
            <td>
                <?= $this->partial(
                    'stuki-workspace/index/partial/revision-approve',
                    array('revision' => $association->getRevision())
                );
                ?>
            </td>
        </tr>
        <?php
        }
        ?>
    </tbody>
</table>
