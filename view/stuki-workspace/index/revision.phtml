<a class="btn btn-info pull-right" style="margin-left: 20px;" href="<?=
    $this->url('stuki-workspace/master');
?>"><i class="icon-code-fork"></i> Master</a>

<a class="btn btn-info pull-right" href="<?=
    $this->url('stuki-workspace/firehose');
?>"><i class="icon-bullseye"></i> Firehose</a>

<h1>Workspace Revision</h1>

<table class="table table-striped table-hover table-bordered">
    <caption>
        Revision
    </caption>
    <thead>
        <tr>
            <th>Revision</th>
            <th>Comment</th>
            <th>User</th>
            <th>Approval</th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td>
                <?= $this->escapeHtml($this->revision->getId());?>

                @ <?php echo $this->StukiDateTimeFormatter($this->revision->getTimestamp()); ?>
            </td>

            <td><?= $this->escapeHtml($this->revision->getComment()); ?></td>

            <td>
                <?= $this->partial(
                    'stuki-workspace/index/partial/user-button',
                    array('user' => $this->revision->getUser())
                );?>
            </td>
            <td>
                <?= $this->partial(
                    'stuki-workspace/index/partial/revision-approve',
                    array('revision' => $this->revision)
                );
                ?>
            </td>
        </tr>
    </tbody>
</table>


<?php
foreach ($this->revision->getRevisionEntities() as $revisionEntity) {
?>

<?= $this->stukiRevisionEntityLink($revisionEntity); ?>

<table class="table table-striped table-hover table-bordered ">
    <thead>
        <tr>
            <th>Field</th>
            <th>Value</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($this->stukiWorkspaceService()->getEntityValues($revisionEntity->getAuditEntity()) as $field => $value): ?>
            <tr>
                <td><?php  echo $this->escapeHtml($field); ?></td>
                <td>
                    <?php if ($value instanceof \DateTime): ?>
                        <?php echo  $this->StukiDateTimeFormatter($value); ?>
                    <?php else : ?>
                        <?php  echo $this->escapeHtml($value); ?>
                    <?php endif; ?>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>


<?php
}
