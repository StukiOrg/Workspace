                <a href="<?php
                    if ($this->user) {
                        echo $this->url('workspace/user', array('userId' => $this->user->getId()));
                    } else {
                        echo $this->url('workspace/user', array('userId' => '0'));
                    }
                ?>" class="btn btn-default">
                    <?php
                        if ($this->user) {
                             echo $this->gravatar($this->user->getEmail(), array('img_size' => 18));
                            echo ' ';
                            echo $this->escapeHtml($this->user->getDisplayName());
                        } else {
                            echo '<i class="icon-globe"></i> Workspace';
                        }
                    ?>
                </a>
